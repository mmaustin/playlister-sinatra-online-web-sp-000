<%= @artist.name%>
<% @artist.songs.each do |s|%>
    <a href=<%= "/songs/#{s.slug}"%>><%= s.name%></a><br>
<%end%>
<% @artist.genres.each do |g|%>
    <a href=<%= "/genres/#{g.slug}"%>><%= g.name%></a><br>
<%end%>